import type { Config } from 'prettier';

const config: Config = {
  // Core formatting rules
  printWidth: 100,
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  bracketSpacing: true,
};

export default config;
